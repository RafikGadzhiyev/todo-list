import { AppTitle, HeaderContent } from "./StyledComponents"

export const HeaderComponent: React.FC = () => {
    return <HeaderContent>
        <AppTitle>
            #todo
        </AppTitle>
    </HeaderContent>
}